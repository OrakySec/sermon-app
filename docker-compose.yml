version: '3.8'

services:
  sermon-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sermon-app
    restart: always
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # If you have a reverse proxy like Traefik or Nginx Proxy Manager, you can add labels/networks here:
    # networks:
    #   - web
    # labels:
    #   - "traefik.enable=true"
    #   - "traefik.http.routers.sermonapp.rule=Host(`sermon.ykaromarques.com`)"
    #   - "traefik.http.services.sermonapp.loadbalancer.server.port=3000"

# networks:
#   web:
#     external: true
